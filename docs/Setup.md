# Setup Guide

Refer to the root `README.md` for standard setup.

## Environment Variables
- Copy `backend/.env.example` to `backend/.env` and update the values.
- If using Docker, these variables are injected into the containers automatically.
