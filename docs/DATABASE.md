# Database Documentation

## Schema
*To be generated once models are implemented.*

## Setup
The primary database is PostgreSQL 15.
Connection parameters are managed via the `.env` file (e.g., `DATABASE_NAME`, `DATABASE_USER`).
